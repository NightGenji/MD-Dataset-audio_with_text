from constants.constants import SEGMENTS, TEXT_SEG, ID_SEG

# WORDS = ['procente']  # %
# WORDS = ['kilograme', 'kilometri']  # kg, km

# TODO Use converting function if needed for [ă, ...]
# WORDS = ['unu', 'doi', 'trei', 'patru', 'mie', 'mii', 'sută', ...]  # numere

def find_numbers(data):
    print("Function find_numbers() is not finished")
    return []
    ret = []

    for item in data[SEGMENTS]:
        if ...:
            ret.append(item[ID_SEG])

    return ret